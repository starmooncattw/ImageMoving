# Image Moving Tools — 開發規劃

## 現有工具

| 檔案 | 功能 |
|------|------|
| `AnimationHTML_Generator.html` | 上傳圖片，設定移動/旋轉/縮放/濾鏡/粒子，輸出獨立 HTML 動畫檔 |
| `PhysicsGIF_Studio.html` | 上傳圖片，套用物理動態效果，輸出 GIF |
| `GIF_Editor.html` | GIF 幀編輯器，支援幀管理、延遲調整、插入圖片、串接 GIF、輸出 GIF |

---

## 工具規劃

### 1. GIF Editor（GIF 幀編輯器）— `GIF_Editor.html` ✅

**定位：幀結構操作，不做視覺合成**

**已完成功能**
- 上傳 GIF → 解析所有幀，縮圖列表顯示
- 刪除、複製、拖曳重排幀
- 調整全部或個別幀的延遲時間（套用本幀／全部，或批量 +/- 指定 ms）
- 輸出尺寸縮放
- 輸出成新 GIF
- 反轉幀順序
- Undo（最多 10 步，Ctrl+Z，輸入框內不觸發）
- 單幀下載（PNG）、單幀取代（上傳圖片）
- 幀管理器：多選勾選、批量刪除／複製／延遲調整
- 幀管理器：多選下載（1 張 PNG / 多張打包 ZIP）
- 幀管理器：插入多張圖片（PNG/JPG/WebP，依檔名自然排序）
- 幀管理器：多選幀前移／後移一格（← 前移 / 後移 →）
- 幀管理器：多選拖曳重排（拖曳任一選取幀，整組一起移動）
- 串接 GIF：插入另一個 GIF 的所有幀，保留原始 delay，尺寸不同時提示選擇（拉伸／等比補透明／等比裁切）
- 幀列表滾輪水平捲動（滑鼠滾輪上下 → 左右捲動幀列表）

---

### 2. GIF Combiner（GIF 疊加合成器）— `GIF_Combiner.html` ✅

**定位：多圖層視覺疊加合成**

**已完成功能**
- 上傳多個 GIF / PNG / JPG / WebP 作為圖層，畫布上即時合成預覽
- 圖層清單：新增、刪除、上移／下移排序
- 畫布上直接拖曳圖層移動位置、拖曳控制點縮放／旋轉
- 圖片圖層去背色（滴管取色 + 容許值），混合模式（Normal／Screen／Multiply）
- 多 GIF 疊加時間軸對齊：以幀數最多的 GIF 為基準，其餘依時間補幀，短的循環播放
- 文字圖層：字體/大小/顏色/粗體/描邊/陰影、手動換行與自動換行、出現幀範圍限制
- 梗圖排版模板：一鍵套用上下大白字＋黑邊
- 濾鏡圖層：亮度/對比/色相/飽和度，非破壞性即時套用於下方圖層
- 粒子圖層：8 種粒子效果（下雪/愛心/星星/泡泡/櫻花/閃光/螢火蟲/彩紙，移植自 PhysicsGIF_Studio），全畫布疊加、可調速度與混合模式
- 圖層上下移動：清單內按鈕與滑鼠拖曳排序皆支援
- 選取圖層後可用滑鼠滾輪縮放
- 上傳動態圖層後自動播放預覽
- 輸出合成結果為新 GIF（沿用時間軸幀數與各幀延遲）

**上傳支援**
- GIF（動態圖層，逐幀播放）
- PNG / JPG / WebP（靜態圖層，每幀顯示同一張）

**圖層類型**

| 類型 | 說明 |
|------|------|
| 圖片圖層 | GIF / PNG / JPG，可設位置、縮放、不透明度，畫布上直接拖曳移動／縮放／旋轉；可設去背色（去除指定顏色轉為透明）；可設混合模式（Normal／Screen／Multiply） |
| 文字圖層 | 字體、大小、顏色、粗體、描邊、陰影，可設出現幀範圍，畫布上直接拖曳文字框調整位置與大小 |
| 濾鏡圖層 | 亮度、對比、色相、飽和度，非破壞性，export 時套用 |

**多 GIF 疊加時間軸**
- 以幀數最多／FPS 最高的 GIF 為基準時間軸
- FPS 較低的 GIF 依時間對齊補幀（同一幀延長顯示，直到下一幀的時間點）
- 短的 GIF（幀數不足）循環播放補滿

**梗圖向功能（迷因製作）**
- 經典梗圖排版模板 — 上下兩行大白字＋黑邊，一鍵套用
- 文字自動置中、自動換行
- 素材由使用者自行上傳，不內建貼圖／表情包素材庫

**其他細節**
- 圖層順序可拖曳調整
- 輸出幀數以最長的 GIF 為基準，短的循環播放
- 靜態圖片自動複製到每幀
- 即時合成預覽（Canvas 逐幀合成）

**常見應用場景**
- GIF 加浮水印 / Logo
- GIF 加背景圖
- GIF 加文字字幕

---

### 3. GIF SideBySide（GIF 並排工具）— `GIF_SideBySide.html` 待開發

**定位：多個 GIF 並排同步播放並輸出**

**功能**
- 上傳多個 GIF / PNG / JPG
- 選擇排列方向（左右 / 上下）
- 設定間距、對齊方式
- 多畫布同步播放即時預覽
- 輸出為拼接後的單一 GIF

**常見應用場景**
- 多個 GIF 並排比較
- Before / After 對比動圖

---

### 2-a. 串接功能（併入 GIF_Editor）

**定位：擴充現有 GIF_Editor，支援插入整個 GIF 的所有幀**

- 在幀管理器「插入」功能中，新增支援上傳 GIF 檔案
- 解析 GIF 所有幀後依序插入至指定位置
- UI 與現有插入圖片流程相同，無需額外介面

---

### 4. Image_Toolbox（圖片編輯工具）— 待開發

**定位：處理裁切、旋轉、翻轉，支援靜態圖與 GIF**

**支援格式**
- GIF（裁切後重新輸出 GIF，所有幀套用相同裁切範圍）
- PNG / JPG / WebP（裁切後輸出原格式）

**操作方式**
- 上傳圖片後顯示於畫布，過大的圖片自動縮放適應畫面（實際裁切仍以原始像素計算，不損失畫質）
- 拖曳選取框決定裁切區域，同步顯示 X / Y / 寬 / 高數值
- 裁切框最小尺寸限制為 10x10 像素，避免拉成 0x0
- 數值也可直接輸入，選取框自動更新
- 提供常用比例快速套用按鈕（1:1、16:9、4:3 等），預設仍為自由裁切
- 提供旋轉（左轉 90° / 右轉 90°）與水平 / 垂直翻轉按鈕
- 即時預覽裁切結果
  - GIF 拖曳裁切框時，即時預覽只重繪第一幀，避免多幀即時重繪造成卡頓
  - 確定裁切後，才將同一裁切範圍套用到所有幀並重新輸出
- 提供「重置」按鈕，一鍵回到剛上傳時的原始狀態（清除裁切框、旋轉、翻轉）
- 匯出裁切後的檔案，檔名為「原檔名 + _edited」（例如 cat.png → cat_edited.png）
- JPG 輸出固定使用高畫質（92%），不開放畫質調整選項
- 僅支援電腦滑鼠操作，不特別支援手機/平板觸控
- 上傳不支援的格式時顯示提示訊息（「不支援此格式，請上傳 PNG/JPG/WebP/GIF」），不允許上傳

**技術需求**
- `omggif` — GIF 解碼拆幀（與其他工具共用）
- `gif.js` — GIF 重新編碼輸出

---

## 技術共用說明

- **omggif** — 解碼 GIF、拆出每幀 ImageData
- **gif.js** — 重新編碼輸出 GIF（參考 PhysicsGIF_Studio 的內嵌方式）

---

## 部署方式

- 純靜態 HTML，所有處理在瀏覽器本機完成，圖片不上傳伺服器
- 托管於 GitHub Pages：`https://starmooncattw.github.io/ImageMoving/`
- `index.html` 作為工具集入口頁

---

## 更新紀錄

- **2026-09-06：SEO 基礎建設與創作者工具箱導流升級**
  - 新增 `robots.txt` 與 `sitemap.xml`（收錄首頁與 5 款工具頁面）
  - 首頁 `index.html` 補齊完整 SEO Meta Tags（Canonical、Description、Keywords、Open Graph 社交預覽卡片、Twitter Cards 與 Schema.org `WebApplication` 結構化資料）
  - 全站右下角升級為「🧪 創作者工具箱」互動彈窗（整合 Discord 專屬直達邀請碼 `https://discord.gg/weddpRK8N8`、綠界贊助、以及連往姊妹專案 `StreamSchedule` 與 `ThumbLayerStudio` 之互通導流）
  - 同步更新 `宣傳用內容/promo_text.txt` 文案庫之 Discord 交流區連結與委託說明（加註於群內留言勿直接私訊）

